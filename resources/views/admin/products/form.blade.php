<form method="POST" action="{{ $action }}" enctype="multipart/form-data" class="form">@csrf @if($method === 'PUT') @method('PUT') @endif<select name="category_id" required>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id', $product->category_id ?? '') == $category->id)>{{ $category->name }}</option>@endforeach</select><input name="name" placeholder="Название" value="{{ old('name', $product->name ?? '') }}" required><textarea name="description" placeholder="Описание" required>{{ old('description', $product->description ?? '') }}</textarea><input name="price" type="number" step="0.01" placeholder="Цена" value="{{ old('price', $product->price ?? '') }}" required><input name="quantity" type="number" placeholder="Количество" value="{{ old('quantity', $product->quantity ?? 0) }}" required><input name="image" type="file"><label><input name="is_active" type="checkbox" value="1" @checked(old('is_active', $product->is_active ?? true))> Активен</label><button class="btn">Сохранить</button></form>
