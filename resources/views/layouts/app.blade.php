<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>{{ config('app.name','MiniShop') }}</title><link rel="stylesheet" href="{{ asset('css/style.css') }}"></head>
<body><header class="topbar"><a class="logo" href="{{ route('home') }}">MiniShop</a><nav><a href="{{ route('products.index') }}">Каталог</a><a href="{{ route('cart.index') }}">Корзина <span id="cart-count">{{ array_sum(array_column(session('cart', []), 'quantity')) }}</span></a>@auth<a href="{{ route('orders.index') }}">Мои заказы</a>@if(auth()->user()->role === 'admin')<a href="{{ route('admin.dashboard') }}">Админка</a>@endif<form method="POST" action="{{ route('logout') }}" class="inline">@csrf<button>Выход</button></form>@else<a href="{{ route('login') }}">Вход</a><a href="{{ route('register') }}">Регистрация</a>@endauth</nav></header><main class="container">@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert error">{{ $errors->first() }}</div>@endif @yield('content')</main><script src="{{ asset('js/app.js') }}"></script></body></html>
